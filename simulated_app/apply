#!/usr/bin/env bash
# Thin wrapper so you can run: ./apply apply.sql
set -euo pipefail
ROOT="$(cd "$(dirname "$0")" && pwd)"
cd "$ROOT"
if [[ -x "$ROOT/.venv/bin/python" ]]; then
  exec "$ROOT/.venv/bin/python" "$ROOT/apply.py" "$@"
fi
exec python3 "$ROOT/apply.py" "$@"
