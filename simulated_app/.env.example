# Default: SQLite (no extra setup)
DB_BACKEND=sqlite

# --- Postgres ---
# DB_BACKEND=postgres
# DATABASE_URL=postgresql://postgres:password@localhost:5432/simulated_app
#
# Or instead of DATABASE_URL:
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=simulated_app
# PGUSER=postgres
# PGPASSWORD=password
